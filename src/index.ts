
// Draw the board
//
// Draw the row slots
let emptyRow: string = "";
for (let i: number = 0; i < 16; ++i)
    emptyRow += ' ';
// Draw the rows
for (let i: number= 0; i < 10; ++i)
    console.log(emptyRow);
